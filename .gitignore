# Rust build artifacts
/target/
**/*.rs.bk
*.pdb

# TUI-Lab run artifacts
.tui-lab/
*.cast
*.apng
*.gif
*.mp4
# Root-level coverage OUTPUT directory (e.g. llvm-cov html/). MUST be
# anchored: the unanchored `coverage/` was matching `src/coverage/` — the
# crate's own coverage module — silently untracking ALL of it.
/coverage/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Keep test fixtures
!/fixtures/

# nested leftover scaffold repo (not part of this crate)
/tui-lab/
fixtures/__pycache__/
